;;-*-Lisp-*-
(in-package goal)

;; name: cam-states.gc
;; name in dgo: cam-states
;; dgos: GAME, ENGINE

;; TODO - for cam-start
(define-extern cam-string state)
(define-extern *camera-base-mode* state)
