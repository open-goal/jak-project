;;-*-Lisp-*-
(in-package goal)

;; name: main.gc
;; name in dgo: main
;; dgos: GAME, ENGINE

(defun movie? ()
  "Are we in a movie?"
  (nonzero? (logand (-> *kernel-context* prevent-from-run) (process-mask movie)))
  )
