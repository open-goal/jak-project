;;-*-Lisp-*-
(in-package goal)


(defenum speedrun-category
  ;; Main Categories
  (full-game-any 0)       ;; Any% (placeholder for any full game run for now)
  (full-game-100 1)       ;; 100% UNUSED
  (full-game-noLTS 2)     ;; No Lava Tube skip UNUSED
  (full-game-noFCS 3)     ;; No Fire Canyon skip UNUSED
  (full-game-orbless 4)   ;; Orbless UNUSED
  (full-game-all-flies 5) ;; All Flies UNUSED
  (full-game-all-orbs 6)  ;; All Orbs UNUSED

  ;; Individual Levels
  (il-training 7)         ;; Geyser Rock IL
  (il-village1 8)         ;; Sandover Village IL
  (il-beach 9)            ;; Sentinel Beach IL
  (il-jungle 10)          ;; Forbidden Jungle IL
  (il-misty 11)           ;; Misty Island IL
  (il-firecanyon 12)      ;; Fire Canyon IL
  (il-village2 13)        ;; Rock Village IL
  (il-sunken 14)          ;; Lost Precursor City IL
  (il-swamp 15)           ;; Boggy Swamp IL
  (il-rolling 16)         ;; Precursor Basin IL
  (il-ogre 17)            ;; Mountain Pass IL
  (il-village3 18)        ;; Volcanic Crater IL
  (il-snow 19)            ;; Snowy Mountain IL
  (il-cave 20)            ;; Spider Cave IL
  (il-lavatube 21)        ;; Lava Tube IL
  (il-citadel 22)         ;; Gol and Maia's Citadel IL

  ;; Extension Categories (TBD)
  )


(deftype speedrun-info-jak1 (structure)
  (
   (category speedrun-category)
   (needs-setup? symbol)
   )
  )

(define-extern start-speedrun (function speedrun-category none))
(define-extern setup-speedrun (function none))
(define-extern speedrun-mode-update (function none))
(define-extern speedrun-draw-settings (function none))