;;-*-Lisp-*-
(in-package goal)

(define *autosplit-info-jak1* (new 'global 'autosplit-info-jak1))

(defun update-autosplit-info-jak1 ()
  ;; general statistics
  (set! (-> *autosplit-info-jak1* num-power-cells) (&-> *game-info* fuel))
  (set! (-> *autosplit-info-jak1* num-orbs) (&-> *game-info* money-total))
  (set! (-> *autosplit-info-jak1* num-scout-flies) (&-> *game-info* buzzer-total))

  ;; loading/cutscene related flags
  (set! (-> *autosplit-info-jak1* in-cutscene?) (-> *pc-settings* movie?))

  ;; need resolution flags
  (set! (-> *autosplit-info-jak1* res-jungle-eggtop) (task-closed? (game-task jungle-eggtop) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-jungle-lurkerm) (task-closed? (game-task jungle-lurkerm) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-jungle-tower) (task-closed? (game-task jungle-tower) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-jungle-fishgame) (task-closed? (game-task jungle-fishgame) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-jungle-plant) (task-closed? (game-task jungle-plant) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-jungle-buzzer) (task-closed? (game-task jungle-buzzer) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-jungle-canyon-end) (task-closed? (game-task jungle-canyon-end) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-jungle-temple-door) (task-closed? (game-task jungle-temple-door) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village1-yakow) (task-closed? (game-task village1-yakow) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village1-mayor-money) (task-closed? (game-task village1-mayor-money) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village1-uncle-money) (task-closed? (game-task village1-uncle-money) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village1-oracle-money1) (task-closed? (game-task village1-oracle-money1) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village1-oracle-money2) (task-closed? (game-task village1-oracle-money2) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-beach-ecorocks) (task-closed? (game-task beach-ecorocks) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-beach-pelican) (task-closed? (game-task beach-pelican) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-beach-flutflut) (task-closed? (game-task beach-flutflut) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-beach-seagull) (task-closed? (game-task beach-seagull) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-beach-cannon) (task-closed? (game-task beach-cannon) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-beach-buzzer) (task-closed? (game-task beach-buzzer) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-beach-gimmie) (task-closed? (game-task beach-gimmie) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-beach-sentinel) (task-closed? (game-task beach-sentinel) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-misty-muse) (task-closed? (game-task misty-muse) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-misty-boat) (task-closed? (game-task misty-boat) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-misty-warehouse) (task-closed? (game-task misty-warehouse) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-misty-cannon) (task-closed? (game-task misty-cannon) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-misty-bike) (task-closed? (game-task misty-bike) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-misty-buzzer) (task-closed? (game-task misty-buzzer) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-misty-bike-jump) (task-closed? (game-task misty-bike-jump) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-misty-eco-challenge) (task-closed? (game-task misty-eco-challenge) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village2-gambler-money) (task-closed? (game-task village2-gambler-money) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village2-geologist-money) (task-closed? (game-task village2-geologist-money) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village2-warrior-money) (task-closed? (game-task village2-warrior-money) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village2-oracle-money1) (task-closed? (game-task village2-oracle-money1) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village2-oracle-money2) (task-closed? (game-task village2-oracle-money2) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-swamp-billy) (task-closed? (game-task swamp-billy) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-swamp-flutflut) (task-closed? (game-task swamp-flutflut) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-swamp-battle) (task-closed? (game-task swamp-battle) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-swamp-tether-1) (task-closed? (game-task swamp-tether-1) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-swamp-tether-2) (task-closed? (game-task swamp-tether-2) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-swamp-tether-3) (task-closed? (game-task swamp-tether-3) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-swamp-tether-4) (task-closed? (game-task swamp-tether-4) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-swamp-buzzer) (task-closed? (game-task swamp-buzzer) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-sunken-platforms) (task-closed? (game-task sunken-platforms) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-sunken-pipe) (task-closed? (game-task sunken-pipe) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-sunken-slide) (task-closed? (game-task sunken-slide) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-sunken-room) (task-closed? (game-task sunken-room) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-sunken-sharks) (task-closed? (game-task sunken-sharks) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-sunken-buzzer) (task-closed? (game-task sunken-buzzer) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-sunken-top-of-helix) (task-closed? (game-task sunken-top-of-helix) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-sunken-spinning-room) (task-closed? (game-task sunken-spinning-room) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-rolling-race) (task-closed? (game-task rolling-race) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-rolling-robbers) (task-closed? (game-task rolling-robbers) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-rolling-moles) (task-closed? (game-task rolling-moles) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-rolling-plants) (task-closed? (game-task rolling-plants) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-rolling-lake) (task-closed? (game-task rolling-lake) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-rolling-buzzer) (task-closed? (game-task rolling-buzzer) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-rolling-ring-chase-1) (task-closed? (game-task rolling-ring-chase-1) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-rolling-ring-chase-2) (task-closed? (game-task rolling-ring-chase-2) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-snow-eggtop) (task-closed? (game-task snow-eggtop) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-snow-ram) (task-closed? (game-task snow-ram) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-snow-fort) (task-closed? (game-task snow-fort) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-snow-ball) (task-closed? (game-task snow-ball) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-snow-bunnies) (task-closed? (game-task snow-bunnies) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-snow-buzzer) (task-closed? (game-task snow-buzzer) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-snow-bumpers) (task-closed? (game-task snow-bumpers) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-snow-cage) (task-closed? (game-task snow-cage) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-firecanyon-buzzer) (task-closed? (game-task firecanyon-buzzer) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-firecanyon-end) (task-closed? (game-task firecanyon-end) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-citadel-sage-green) (task-closed? (game-task citadel-sage-green) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-citadel-sage-blue) (task-closed? (game-task citadel-sage-blue) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-citadel-sage-red) (task-closed? (game-task citadel-sage-red) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-citadel-sage-yellow) (task-closed? (game-task citadel-sage-yellow) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village3-extra1) (task-closed? (game-task village3-extra1) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village1-buzzer) (task-closed? (game-task village1-buzzer) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village2-buzzer) (task-closed? (game-task village2-buzzer) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village3-buzzer) (task-closed? (game-task village3-buzzer) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-cave-gnawers) (task-closed? (game-task cave-gnawers) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-cave-dark-crystals) (task-closed? (game-task cave-dark-crystals) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-cave-dark-climb) (task-closed? (game-task cave-dark-climb) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-cave-robot-climb) (task-closed? (game-task cave-robot-climb) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-cave-swing-poles) (task-closed? (game-task cave-swing-poles) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-cave-spider-tunnel) (task-closed? (game-task cave-spider-tunnel) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-cave-platforms) (task-closed? (game-task cave-platforms) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-cave-buzzer) (task-closed? (game-task cave-buzzer) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-ogre-boss) (task-closed? (game-task ogre-boss) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-ogre-end) (task-closed? (game-task ogre-end) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-ogre-buzzer) (task-closed? (game-task ogre-buzzer) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-lavatube-end) (task-closed? (game-task lavatube-end) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-lavatube-buzzer) (task-closed? (game-task lavatube-buzzer) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-citadel-buzzer) (task-closed? (game-task citadel-buzzer) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-training-gimmie) (task-closed? (game-task training-gimmie) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-training-door) (task-closed? (game-task training-door) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-training-climb) (task-closed? (game-task training-climb) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-training-buzzer) (task-closed? (game-task training-buzzer) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village3-miner-money1) (task-closed? (game-task village3-miner-money1) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village3-miner-money2) (task-closed? (game-task village3-miner-money2) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village3-miner-money3) (task-closed? (game-task village3-miner-money3) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village3-miner-money4) (task-closed? (game-task village3-miner-money4) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village3-oracle-money1) (task-closed? (game-task village3-oracle-money1) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-village3-oracle-money2) (task-closed? (game-task village3-oracle-money2) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-swamp-arm) (task-closed? (game-task swamp-arm) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-red-eggtop) (task-closed? (game-task red-eggtop) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-lavatube-balls) (task-closed? (game-task lavatube-balls) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-intro) (task-closed? (game-task intro) (task-status need-resolution)))
  (set! (-> *autosplit-info-jak1* res-ogre-secret) (task-closed? (game-task ogre-secret) (task-status need-resolution)))

  ;; need introduction tasks
  (set! (-> *autosplit-info-jak1* int-finalboss-movies) (task-closed? (game-task finalboss-movies) (task-status need-introduction)))

  ;; end
  (none))
