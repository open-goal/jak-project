;;-*-Lisp-*-
(in-package goal)

;; name: target2.gc
;; name in dgo: target2
;; dgos: ENGINE, GAME

;; DECOMP BEGINS

(defstate target-clone-anim (target)
  :code (behavior ((hnd handle))
    (looping-code))
  )