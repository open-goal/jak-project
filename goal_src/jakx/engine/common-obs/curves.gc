;;-*-Lisp-*-
(in-package goal)

;; name: curves.gc
;; name in dgo: curves
;; dgos: ENGINE, GAME

;; +++loop-behavior
(defenum loop-behavior
  :type uint64
  :bitfield #f
  (wrap)
  (clamp)
  (b2)
  (use-default)
  )
;; ---loop-behavior


;; DECOMP BEGINS

