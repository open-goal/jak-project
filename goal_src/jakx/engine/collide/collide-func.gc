;;-*-Lisp-*-
(in-package goal)

;; name: collide-func.gc
;; name in dgo: collide-func
;; dgos: ENGINE, GAME

;; DECOMP BEGINS

;; ERROR: function was not converted to expressions. Cannot decompile.

;; ERROR: failed type prop at 6: Called a function, but we do not know its type
;; WARN: Return type mismatch none vs float.
(defun ray-sphere-intersect ((a0-0 vector) (a1-0 vector) (a2-0 vector) (a3-0 float))
  (local-vars (v0-0 none) (t9-0 function))
  (rlet ((vf1 :class vf)
         (vf2 :class vf)
         )
    (.lvf vf1 (&-> a0-0 quad))
    (.lvf vf2 (&-> a2-0 quad))
    (.sub.vf vf1 vf1 vf2)
    (.lvf vf2 (&-> a1-0 quad))
    (set! t9-0 raw-ray-sphere-intersect)
    (set! a0-1 a3-0)
    (set! v0-0 (the-as none (call!)))
    (ret-value v0-0)
    )
  )

;; ERROR: failed type prop at 9: Called a function, but we do not know its type
;; WARN: Return type mismatch none vs float.
(defun ray-circle-intersect ((a0-0 vector) (a1-0 vector) (a2-0 vector) (a3-0 float))
  (local-vars (v0-0 none) (t9-0 function))
  (rlet ((vf0 :class vf)
         (vf1 :class vf)
         (vf2 :class vf)
         )
    (init-vf0-vector)
    (.lvf vf1 (&-> a0-0 quad))
    (.mov.vf.y vf1 vf0)
    (.lvf vf2 (&-> a2-0 quad))
    (.mov.vf.y vf2 vf0)
    (.sub.vf vf1 vf1 vf2)
    (.lvf vf2 (&-> a1-0 quad))
    (.mov.vf.y vf2 vf0)
    (set! t9-0 raw-ray-sphere-intersect)
    (set! a0-1 a3-0)
    (set! v0-0 (the-as none (call!)))
    (ret-value v0-0)
    )
  )

;; ERROR: function was not converted to expressions. Cannot decompile.

(defun ray-plane-intersect ((arg0 vector) (arg1 vector) (arg2 vector) (arg3 vector) (arg4 vector) (arg5 vector) (arg6 vector))
  (local-vars (v1-0 float) (v1-1 float) (a2-1 float))
  (rlet ((acc :class vf)
         (Q :class vf)
         (vf0 :class vf)
         (vf1 :class vf)
         (vf2 :class vf)
         (vf3 :class vf)
         (vf4 :class vf)
         (vf5 :class vf)
         (vf6 :class vf)
         (vf7 :class vf)
         (vf8 :class vf)
         (vf9 :class vf)
         )
    (init-vf0-vector)
    (.lvf vf3 (&-> arg5 quad))
    (.lvf vf1 (&-> arg4 quad))
    (.lvf vf2 (&-> arg6 quad))
    (.sub.vf vf1 vf3 vf1)
    (.sub.vf vf2 vf3 vf2)
    (.lvf vf6 (&-> arg2 quad))
    (.lvf vf7 (&-> arg3 quad))
    (.sub.vf vf8 vf3 vf6)
    (.outer.product.a.vf acc vf2 vf1)
    (.outer.product.b.vf vf4 vf1 vf2 acc)
    (.mul.vf vf8 vf8 vf4)
    (.mul.vf vf9 vf7 vf4)
    (.mul.vf vf5 vf4 vf4)
    (.add.y.vf.x vf8 vf8 vf8)
    (.add.y.vf.x vf9 vf9 vf9)
    (.add.y.vf.x vf5 vf5 vf5)
    (.add.z.vf.x vf8 vf8 vf8)
    (.add.z.vf.x vf9 vf9 vf9)
    (.add.z.vf.x vf5 vf5 vf5)
    (.mov v1-0 vf9)
    (.mov a2-1 vf8)
    (.isqrt.vf Q vf0 vf5 :fsf #b11 :ftf #b0)
    (let ((f1-0 a2-1)
          (f2-0 v1-0)
          )
      (cond
        ((!= f2-0 0.0)
         (let ((f1-1 (/ f1-0 f2-0)))
           (.mov.vf.w vf4 vf0)
           (.wait.vf)
           (.mul.vf.xyz vf4 vf4 Q)
           (let ((v0-0 f1-1))
             (.mov vf8 v0-0)
             (.svf (&-> arg1 quad) vf4)
             (.mul.x.vf acc vf7 vf8)
             (.add.mul.w.vf.xyz vf7 vf6 vf0 acc)
             (.svf (&-> arg0 quad) vf7)
             (.mov v1-1 vf7)
             v0-0
             )
           )
         )
        (else
          -100000000.0
          )
        )
      )
    )
  )

;; ERROR: function was not converted to expressions. Cannot decompile.

(def-mips2c collide-do-primitives function)

(def-mips2c moving-sphere-triangle-intersect (function vector vector float collide-cache-tri vector vector float))

;; ERROR: function has no type analysis. Cannot decompile.

;; ERROR: function has no type analysis. Cannot decompile.
