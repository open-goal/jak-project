(define format _format)

(let ((my-list (list (cons 'a 'b)
                     (cons 'c 'd)
                     (cons 'e 'f)
                     )
               ))
  (delete-car! 'c my-list)
  (format #t "~A~%" my-list)
  (format #t "~A~%" (assoc 'c my-list))
  )
0