(test-setup '(2 3 4 7 9) #f)



(let ((my-list
        (list 24 16 32 56 72)
        ))
  (sort my-list (lambda ((x integer) (y integer)) (- x y)))
  ;;(sort my-list (lambda (x y) (<= x y)))
  (format #t "~A~%" my-list)
  )

