					;-*-Scheme-*-

(test-setup 256 #f)

(defun pow-test ((base float) (exponent integer))
  (let ((result base))
    (while (> exponent 1)
           (*! result base)
           (-! exponent 1)
           )
    result
    )
  )

(format #t "~,,0f~%" (pow-test 2.0 8))
